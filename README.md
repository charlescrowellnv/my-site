# Routes

**Do not create a Next.js route at `/docs`**
Mintlify controls this path. Read here: https://www.mintlify.com/docs/deploy/vercel

## Local development

`next dev` does not apply `vercel.json` rewrites.
Use `vercel dev` to test `/docs` locally.
